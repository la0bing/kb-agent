# Agent framework (chat UI via `adk web`)
google-adk
litellm

# LLM served locally over an OpenAI-compatible API
transformers[serving]
accelerate

# Document parsing
docling

# Embeddings (BAAI/bge-small-en-v1.5)
sentence-transformers

# Embedded vector store, no server
lancedb

numpy
pyarrow
python-dotenv
